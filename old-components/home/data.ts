/**
 * Deprecated.
 *
 * The public branch experience is now powered by the shared branch domain model in
 * `components/branches/*`, not by static home mock constants.
 *
 * Legacy references should migrate to:
 * - `getPublicBranchPageDataBySlug`
 * - `getPublicBranchesIndex`
 * - `/app/locations/*`
 */

export const HOME_DATA_DEPRECATED = true;
